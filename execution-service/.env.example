# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=etl_engine

# Delta Lake Configuration
DELTA_LAKE_BASE_PATH=./delta-lake

# Spark Configuration
SPARK_APP_NAME=ETL-Executor
SPARK_MASTER=local[*]

# API Configuration
API_V1_PREFIX=/api/v1
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# Security (IMPORTANT: Generate a Fernet key using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=your-fernet-key-here
